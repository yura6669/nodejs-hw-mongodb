description: 'Not found'
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 404
        message:
          type: string
          example: 'NotFoundError'
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Resource not found. Please check the ID or URL and try again.'
